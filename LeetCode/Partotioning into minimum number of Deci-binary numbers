class Solution {
    public int minPartitions(String n) {
        int max=0;
        for(int ch:n.toCharArray()){
            int num=ch-'0';
            max=Math.max(num,max);
        }
        return max;
    }
}
